# Drafts — frozen snapshots
*(Same convention as The Deep: before any major revision, snapshot the current files into `YYYY-MM-DD_vN/`. Nothing is ever lost. "save a draft" / "restore [snapshot]" are the phrases.)*
