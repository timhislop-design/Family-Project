# Parked for Later

Good ideas we're not doing yet, so we don't lose them.

## Mentor authors / influences — BUILT June 19, 2026
The framework is now in place (`shared/voices.md` + per-project Influences in each brief). We're still doing **voice-building first** — the structure is just ready when she is.

Phrases: **"add a voice"**, **"voice session"**, **"lean this one on [author]"**.

## Abby's review/feedback system — BUILT (Jul 10, 2026)
The site lives at repo root (`index.html` + `site/`): premise home with character art, chapter reader (renders the real markdown — no copies), character workspaces with open questions, reader settings panel (text size / theme / easy-read font / spacing), and feedback boxes on every page that file Google Docs into Drive (`The Deep — Feedback/...`) and email Tim per submission. **Tim's one-time setup remains:** `site-setup/SETUP.md` (GitHub repo + Pages + Apps Script paste).
